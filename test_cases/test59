class A {
    static function main() {
        
        function minmax(a, b, min) {
          if (min && a < b || !min && a > b)
            return true;
          else
            return false;
        }
        
        function yote() {
          return (minmax(10, 100, true) && minmax(5, 3, false));
        }

        return yote()
    }
}